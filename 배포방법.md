# 배포 방법 (터미널/코딩 지식 없이 가능)

이 폴더 안에는 파일이 2개 있어요.
- `index.html` : 기존 트래커 (자동조회 부분만 프록시를 쓰도록 수정됨)
- `api/lookup.js` : 로스트아크 API를 대신 호출해주는 작은 서버 함수 (CORS 우회용)

이 두 파일을 통째로 GitHub → Vercel 순서로 올리면 끝입니다. 전부 마우스 클릭으로 가능해요.

## 1. GitHub 계정 만들기 (이미 있으면 생략)
1. https://github.com 접속 → Sign up
2. 이메일/비밀번호로 가입

## 2. 새 저장소(repository) 만들기
1. 로그인 후 오른쪽 위 `+` 버튼 → `New repository`
2. Repository name에 `loa-guild-tracker` 입력
3. Public으로 두고 `Create repository` 클릭

## 3. 파일 업로드
1. 방금 만든 저장소 페이지에서 `Add file` → `Upload files` 클릭
2. 이 폴더 안의 `index.html`을 끌어다 놓기
3. 같은 화면에서 `api` 폴더도 만들어야 하는데, GitHub 웹 업로드에서는 폴더째로 끌어놓으면 자동으로 경로가 유지됩니다 — `api` 폴더 전체(`lookup.js` 포함)를 함께 끌어다 놓으세요
4. 아래 `Commit changes` 클릭

## 4. Vercel 가입 및 배포
1. https://vercel.com 접속 → `Sign Up` → **Continue with GitHub** 선택 (방금 만든 GitHub 계정으로 로그인)
2. 로그인 후 `Add New...` → `Project` 클릭
3. 방금 만든 `loa-guild-tracker` 저장소를 찾아서 `Import` 클릭
4. 설정은 그대로 두고 `Deploy` 클릭 (1~2분 소요)
5. 배포가 끝나면 `xxxx.vercel.app` 형태의 주소가 나옵니다 — 이게 앞으로 공대원들과 공유할 최종 주소예요

## 5. 확인
1. 나온 주소로 접속
2. 캐릭터 추가 → API 키 입력 → 캐릭터명 조회
3. 이제 "CORS" 오류 없이 직업/아이템레벨이 자동으로 채워지면 성공입니다

## 참고
- API 키는 각자 브라우저에만 저장되고, 조회할 때만 우리 서버 함수를 거쳐 로스트아크로 전달됩니다. 서버에 따로 저장되지 않아요.
- 나중에 코드를 수정하고 싶으면: GitHub 저장소 → 파일 클릭 → 연필 아이콘(Edit) → 수정 → Commit. 그러면 Vercel이 자동으로 재배포합니다.
